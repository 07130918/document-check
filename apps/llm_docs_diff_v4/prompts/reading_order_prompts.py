"""
読み取り順序推定用のプロンプトテンプレート
"""

READING_ORDER_SYSTEM_PROMPT = """あなたは文書レイアウト解析の専門家です。
与えられた文書要素の配置情報と視覚的特徴から、人間が自然に読む順序を推定してください。

以下の原則に従って判断してください：
1. 基本的には上から下、左から右の順序
2. カラムがある場合は、カラムごとに上から下
3. 見出しやタイトルは本文より先に読まれる
4. 図表のキャプションは図表の直後
5. 矢印や番号がある場合はその順序に従う
6. ライフイベント（入社、結婚、出産、退職など）は論理的な時系列順
7. 視覚的にグループ化された要素は一緒に読まれる
"""

READING_ORDER_USER_PROMPT = """以下の文書要素を自然な読み取り順序に並べ替えてください。

## 文書要素
{elements}

## 視覚的特徴
{visual_features}

## 特別な指示
- インデックス番号だけを正しい順序で返してください
- ライフイベントや時系列的な要素は論理的な順序で並べてください
- 矢印や視覚的な流れがある場合はそれに従ってください

## 出力形式
カンマ区切りのインデックス番号のリスト（例: 0,2,1,4,3）
"""

LIFE_EVENT_DETECTION_PROMPT = """以下のテキスト要素からライフイベントを検出し、論理的な時系列順に並べてください。

## テキスト要素
{elements}

## 一般的なライフイベントの順序
1. 入社/就職
2. 結婚
3. 出産/子供誕生
4. 住宅購入
5. 子供の独立
6. 退職

## 出力形式
検出されたライフイベントとその推奨順序をJSON形式で返してください。
例:
{{
  "life_events": [
    {{"index": 3, "event": "入社", "order": 1}},
    {{"index": 5, "event": "結婚", "order": 2}}
  ]
}}
"""

VISUAL_FLOW_DETECTION_PROMPT = """以下の視覚的特徴から、要素間の接続や流れを検出してください。

## 視覚的特徴
{visual_features}

## 検出すべきパターン
1. 矢印による接続
2. 番号による順序
3. 円形配置（時計回りの流れ）
4. 階層的配置（上位から下位への流れ）
5. フローチャート的な配置

## 出力形式
検出された接続関係をJSON形式で返してください。
例:
{{
  "connections": [
    {{"from": 0, "to": 1, "type": "arrow"}},
    {{"from": 1, "to": 2, "type": "sequential"}}
  ],
  "groups": [
    {{"indices": [3, 4, 5], "pattern": "circular"}}
  ]
}}
"""